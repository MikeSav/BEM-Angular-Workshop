import { MsaInputDirective } from './msa-input.directive';

describe('MsaInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MsaInputDirective();
    expect(directive).toBeTruthy();
  });
});
